1) Можно лис делать какой нибудь дженерик GetAll в дата провайдере?
2) надо ли вообще делать этот датапровайдер? или можно дбконтекст напрямую пихать в контроллеры?
3) почему нельзя инжектить сервисы в методы? нужно только в конструктор и обхявлять локальные поля?
4) если решили делать ViewModel то надо делать из для всего даже если смысла особо нет? например показывать надо то же самое что и в сущности из базы
5) тот же вопрос, только показывать надо меньше? что в таком случае?
6) контроллер работает с IQueryable, а во вьюху передавать IEnumerable? или тоже IQueryable. какие тут бест практисес?
7) есть ли способо создавать вьюмодел лучше чем например сделано в categories?
8) в products таблицу генерировать через рефлексию смотреть какие есть свойства или просто хардкодить тейбл хедер?
9) какой конвеншн по именованию вью моеделей?
10) я немного запутался как например лучше сделать с созданием продукта и редактированием продукта. использовать одну и ту же модель? или создавать разные и под ту и под другую?
11) не понял как прикрутить бутстрап стили к серверсайд валидации формы, чтобы было красиво и разноцветно. например ни класса invalid-feedback, ни is-invalid у меня вообще как я понял нет почему-то
12) для создания и редактирования надо использовать одну и ту же вьюху?
13) свой еррор хендлер надо сдлеать для всех енвайронментов или только для прода?

Task 2.
1) В чем смысл кешировать в файл на компе, все равно ведь это тоже медленно будет? Что из базы эти байты считывать и отдавать клиенту, что с жесткого диска получается не эффективно (вроде).
2) Не нравится куча кастов например в тесте категорий. Но вроде бы другого варианта нет если использовать nunit. Или есть?
3) Как протестить home/error?
4) Image should be sending as binary stream with correct Content-Type (for referencing from HTML pages). 
Я же могу любой экшн метод референсить в своих вьюхах. В чем тут смысл этого замечания for referencing from HTML pages?
5) EditImage view Url.Action можно ли заменить на какой то asp- тег хелпер? Не нашел таких чтобы в src подставить.
6) Почему когда я доавил новый route images поменялся линк в categories/image. я же там явно указываю что action Image
7) Почему при доступе на главную страницу RouteData == null?